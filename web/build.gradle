plugins {
    id "com.moowork.node" version "1.2.0"
}

node {
    version = '8.11.2'
    npmVersion = '4.1.2'
    download = true
}

task buildAssets(type: NpmTask, dependsOn: [npmInstall]) {
    args = ['run', 'build']
}

task runDevServer(type: NpmTask) {
    args = ['run', 'start']
}
